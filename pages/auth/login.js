// components
import Auth from '../../components/Auth'
import Login from '../../components/Login'


const LoginWrapper = () => {
  return (
    <Auth>
      <Login />
    </Auth>
  );
};

export default LoginWrapper;
