// components
import Auth from '../../components/Auth';
import Signup from '../../components/Signup';

const SignupWrapper = () => (
  <Auth>
    <Signup />
  </Auth>
);

export default SignupWrapper;
